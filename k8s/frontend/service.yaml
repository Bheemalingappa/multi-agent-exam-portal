apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: multi-agent-exam
  labels:
    app.kubernetes.io/name: exam-portal-frontend
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: exam-portal-frontend
